import {defaultHealthAuthorityState, HealthAuthorityState} from "./constants";

const healthAuthorityReducer = (
    state: HealthAuthorityState = defaultHealthAuthorityState,
): HealthAuthorityState => {
    return state;
};
export default healthAuthorityReducer;
